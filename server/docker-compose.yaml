version: '3.8'

services:
  financial-server:
    build: .
    container_name: financial-tamagotchi-server
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped